# --- PDF extraction + section/chunk pipeline (steps 1-3) ---
pymupdf==1.28.2       # aka "fitz" — fast full-text extraction (download_and_extract.py, parse_sections.py)
requests==2.32.3      # download the source PDF from clinicaltrials.gov
pdfplumber==0.11.10   # scoped table recovery for Synopsis + Section 3 (recover_tables.py)
pdfminer.six==20260107 # pdfplumber dependency, pinned (older releases break on Python 3.12)

# --- added in later steps ---
# fastapi, uvicorn            (step 4: API)
# sentence-transformers, torch (step 3: local embeddings)
# supabase / psycopg          (step 3: pgvector store)
# anthropic                   (step 4: Claude API)
